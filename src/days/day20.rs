use super::day::*;

pub struct Instance;

impl Day for Instance {
    fn run(&self, _input: String) -> Result<DayResult, String> {
        let part1 = "".to_owned();
        let part2 = None;
        Ok(DayResult { part1, part2 })
    }
}
